# Name of the workflow
name: Code

# When the workflow should run
on:
  push: 
  workflow_call: 

# jobs in the workflow
jobs:
  code-checkout:
    runs-on: ubuntu-latest
    # steps in the wokrflow
    steps:
      - name: Clone the code
        uses: actions/checkout@v7

      - name: Print the message
        run: echo "Code cloned"
